class Contact {
  name: string
  email: string
  tel: string
  id: number
  constructor(name: string, email: string, tel: string, id: number) {
    this.name = name
    this.email = email
    this.tel = tel
    this.id = id
  }
}

export default Contact
